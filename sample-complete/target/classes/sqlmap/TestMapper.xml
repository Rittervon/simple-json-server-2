<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.uracle.sample.api.sample.TestMapper">

    <insert id="createUser" parameterType="com.uracle.sample.api.test.Users">
        insert into users(phone_number, name, password_hash, registration_date)
        values(#{phone_number}, #{name}, #{password_hash}, #{registration_date})
    </insert>

    <select id="selectUserById" resultType="com.uracle.sample.api.test.Users">
        select phone_number, name, password_hash, registration_date
        from users
        where phone_number = #{phone_number}
    </select>

    <select id="selectUser" resultType="com.uracle.sample.api.test.Users">
        select phone_number, name, password_hash, registration_date
        from users
        order by registration_date
    </select>

    <update id="updateUser" parameterType="com.uracle.sample.api.test.Users">
        update users
        set
        <if test="password != null and password != ''">
            password = #{password},
        </if>
        <if test="username != null and username != ''">
            username = #{username},
        </if>
        <if test="company != null and company != ''">
            company = #{company},
        </if>
            moddate = now()
        where id = #{id}
    </update>

    <delete id="deleteSample" parameterType="com.uracle.sample.api.sample.Sample">
        delete from tb_sample
        where id = #{id}
    </delete>
</mapper>